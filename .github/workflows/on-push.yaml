name: on-push
run-name: on push to main

on:
  push:
    branches:
      - main

  # run if user manually requests it
  workflow_dispatch:

concurrency:
  group: live-main-build
  cancel-in-progress: true

jobs:
  test-site:
    permissions:
      contents: read
    uses: ./.github/workflows/test-site.yaml

  build-site:
    needs: test-site
    permissions:
      contents: write
    uses: ./.github/workflows/build-site.yaml
